./smr -setup         for setting container nad enabling `127.0.0.1:8080` to do magic. ž
Do not forget to insert .csv file with email and id column inside `app dir/app/`.
./smr -console          enters the console inside container